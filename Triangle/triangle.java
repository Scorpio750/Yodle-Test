import java.io.*;
import java.util.*;

/**
 * @author patrickwu
 * 
 */

public class Triangle {
	public static void main(Strings args[]) {
		int tempMax, sum = 0;
		Scanner sc = new Scanner(new File("triangle.txt"));

		while (sc.hasNextLine()) {
		// reads through each line, loops over each value and compares to temp max
		}
	}
}
